# מרשם משימות — מחולל הדאבל

עודכן: 14/09/2026 · main = de97664 · dobble.html = 8377 שורות

> כלל: מי שמסיים משימה מעדכן את הקובץ הזה **באותו PR**.
> אם השורה כאן סותרת מה שכתוב בצ'אט — הקובץ הזה קובע.

## 🔴 פתוח עכשיו

| # | נושא | קובץ מושפע | מצב | הבא בתור |
|---|---|---|---|---|
| 19 | רענון מסלול אוטומטי במסלול הקוד | dobble.html | בעבודה, כל בדיקות הדפדפן עברו | PR זה |
| 27 | סקירת כפתורי הוואטסאפ — ארבע נקודות פנייה | dobble.html, js/dobble-config.js | בעבודה, כל שש בדיקות הדפדפן עברו | PR זה |

## ✅ הושלם

| # | נושא | PR | תאריך |
|---|---|---|---|
| מחירון 2026 באתר הציבורי — `events.html` ו-`workshops.html` עודכנו למחירון שהושעיה מסר (חישוק 800→1,100 · שער 600→850 · עמוד 150→220 · שולחנות 30/70/120→45/110/180 · בר 800/600→950/750 · קרנבל 1,300/2,100→1,600/2,500 · חדרים 500/750/1,000→650/950/1,400 · מיילר הקדשה +40→+50 · נסיעות 1.5→2 ₪/ק"מ · מינימום יציאה 500→650). שורת הסדנאות פוצלה לשתיים כי המחירון מבחין ביניהן (חד-פעמית 900, יום הולדת 700). **נותרו במחיר ישן במכוון:** "עמוד נושא" 100 ₪ ו"עיצוב בסיסי — בלוני מיקרו הליום" 40 ₪ — אינם ברשימה שנמסרה, לא הומצא להם מחיר. המחירון עצמו חי ב-`hoshaya-quotes` | — | 23/09/2026 |
| מבנה תמחור 10/15/25 | #123 | 08/09/2026 |
| שחזור קוד גישה — קישור כניסה למייל | #124 | 08/09/2026 |
| הסתרת הדפסה ומשלוח + סידור פאנל לקוחות (אחרונים קודם) | #125 | 08/09/2026 |
| מדידת שימוש ומונה בפאנל | #126 | 09/09/2026 |
| PDF גיליון 330×488 עם בורר קוטר 71/85 (שלב 1) | #127 | 09/09/2026 |
| מערכת הזמנות מדבקות — PR 1/4–3/4 ועוד 5 תיקונים (שכבות חופפות, קו החיתוך #96, `shouldCreateUser`/התראת Realtime #97, PayBox+תפיסת שכבה #98) **מוזגו ל-main**. #96 ו-#98 נגעו שניהם ב-`pencil-stickers.html` והתנגשו רק ב-`מרשם.md` (שני PR עדכנו את אותה שורה) — לא בקוד; נפתר ידנית לפני כל מיזוג, `node --check` על `main` הסופי עובר. **אומת בדפדפן headless אמיתי (Playwright/Chromium), לא רק קריאת קוד:** קו חיתוך אדום מוצג בתצוגה הבודדת וגם בגיליון ✅ · קובץ הדפסה של הלקוח (`buildPrintFile`) ללא קו ✅ · ייצוא PDF של המנהל מכבד את הצ'קבוקס בשני הכיוונים ✅ · טקסט ארוך ("שמואל מרגוליס") נתפס בקצה שמאל וימין, במרחק שהקופסה הישנה (80×40px) הייתה מפספסת ✅ · טקסט קצר עדיין נתפס בקלות ✅ · שכבה מסובבת ב-45° נתפסת נכון ✅ · הידיות מסתגלות לתוכן ✅ · חלונית PayBox נפתחת/נסגרת עם קישור נכון ✅ · אפס שגיאות JS. **נותר לפני פרסום רחב:** בדיקה ידנית ממגע אמיתי בטלפון (השוני בין מגע לקליק עכבר מדויק) | #91–#98 | 31/08 |
| מדבקות — שליטה יחידה על "גובה", אורך נגזר אוטומטית ביחס קבוע 2.8, שלושה כפתורי גודל מהיר, ידית גרירה אנכית-בלבד בתחתית-מרכז. אומת מלא בדפדפן headless — כולל בדיקת הקצה הקריטית (קלאמפ כפול widMm/lenMm) | #99 | 31/08 |
| מדבקות — חצי `−`/`+` סביב שדה הגובה, תוספת לצד השדה/הידית הקיימים (לא תחליף) — תיקון אי-הבנה: הושעיה ביקש חצים ב-#99, קיבל שדה+ידית. אומת עם הקשר מגע אמיתי (Playwright `hasTouch`); בדיקה 8 האמיתית (מגע בטלפון של הושעיה) עדיין פתוחה | #101 | 01/09 |
| מדבקות — מאגר פונטים משותף `js/hebrew-fonts.js` (35 משפחות, 6 קטגוריות, כולן OFL/Apache, כל ה-URL אומתו 200+Hebrew unicode-range) + בורר חצים/תצוגה-חיה במקום `<select>`, עם "כל הפונטים" (טעינה עצלה). משקל הגופן כבד עכשיו `HebrewFonts.hasBold()` במקום `700` קשיח, בהרמוניה עם דיוק התפיסה מ-#98. `HebrewFonts.load()` עטופה ב-`withTimeout` (10s) + `skipToNext()` חסום ל-35 ניסיונות. מוזג על גבי #101, שני ה-PR נגעו ב-`pencil-stickers.html` והתנגשו רק ב-`מרשם.md`. אומת מלא ב-Playwright headless כולל ניתוק רשת אמיתי (flake) ל-Google Fonts בזמן הבדיקה — `skipToNext` טיפל בו כמתוכנן | #100 | 01/09 |
| מדבקות — **דחוף, דיווח לקוחה אמיתית:** אלמנטים חדשים (טקסט/אימוג'י/תמונה) נחתו תמיד באותה נקודה קבועה, נערמים אחד על השני. תוקן ע"י `findFreeSpot()` — בוחר את המועמד הראשון מתוך 9 נקודות שלא מתנגש (מרחק≥0.16) עם שכבה קיימת, נקרא אחרי ניקוי placeholder ישן. חריגה מהמפרט (מדווחת בכוונה): תוקן גם אתר הופעה שלישי (העלאת תמונה) עם אותה תקלה בדיוק, לא הוזכר במפרט המקורי. אומת מלא ב-Playwright headless: 3 אימוג'י/2 טקסטים ברצף בלי חפיפה, placeholder מוחלף לא נערם, 15+ אלמנטים בלי קריסה | #103 | 01/09 |
| מדבקות — מסגרת החיתוך כאובייקט: ידיות צד חופשיות (שמאל/ימין, אמצע-גובה) ליד ידית הגובה הקבועה. `currentRatio` (ברירת מחדל `ASPECT_RATIO=2.8`) הוא היחס הפעיל בפועל — חצים/פריסטים ממשיכים דרך `applyHeight()` (לפי `currentRatio`), גרירת ידית צד משנה `lenMm` ישירות ומחשבת מחדש `currentRatio`. פריסטים מאפסים `currentRatio=ASPECT_RATIO` לפני `applyHeight()`. מוזג על גבי #103 (rebase, לא התנגש בקוד, רק `מרשם.md`). **הערה: שורה זו נשמטה בטעות מהמרשם בסבב קודם — מתועדת כאן בדיעבד** | #102 | 01/09 |
| מדבקות — הסרת חצי בורר הפונטים: התצוגה עצמה (`#font-display-btn`) הופכת ללחיצה שפותחת ישירות את פאנל "כל הפונטים". הוסרו `#font-prev`/`#font-next`/`#font-all-btn` וכל מה שתלוי אך ורק בהם. שתי חריגות מהמפרט: שורת המטא הפכה לרמז קבוע "לחץ לבחירת פונט" (המונה הישן היה דורס את הרמז מיד בטעינה), ו-`e.preventDefault()` נוסף בלחיצת רווח | #104 | 01/09 |
| מדבקות — רוחב תיבת טקסט עצמאי (word-wrap אמיתי) + לחיצה כפולה למיקוד עריכה. שדה חדש `l.boxWidth`, `layoutText()` כמקור אמת יחיד לפריסה (ציור+מדידה). שלוש חריגות מהמפרט שנתפסו בבדיקה בפועל: תוקן fallback שמחק עטיפה אחרי כיווץ, תוקנה נוסחת גרירת רוחב עם יחידות שגויות, חושבו boxX/Y/W/H מחדש ב-dblclick. אומת מלא ב-Playwright כולל תאימות לאחור מלאה להזמנות ישנות בלי `boxWidth` | #105 | 02/09 |
| מדבקות — כלי חדש: עריכה חופשית להדפסה (מנהל בלבד, `?admin=1&mode=free`) + מצב חיתוך "מלבן עם מרווח". `freeLayoutBox()`/`computeStickerBox()` מאוחדות מאחורי `currentEditorBox()` יחיד; `drawLayers()` חולצה מ-`renderStickerContent` כדי לצייר בלי clip. `marginRectFor`/`drawMarginRect`/`buildFreeCutSvg` חדשים. באג אמיתי שנתפס ב-Playwright: `.free-mode-hide` דרס בחזרה הסתרה של `.admin-only` וחשף כלי ניהול לכל אורח — תוקן להוסיף הסתרה בלבד, לעולם לא לבטל. אומת מלא כולל אפס-רגרסיה בזרימת לקוח רגילה | #106 | 02/09 |
| מדבקות — **PR A/2: שני סוגי מדבקות על דף אחד, קו הפרדה גרור** (עורך בלבד — PR B יחבר להגשת הזמנה). ⛔ עקרון המפתח: `layers`/`state` תמיד מייצגים את הסוג שנערך *כרגע* — לא הפכנו אותם למערך עיצובים בכל מקום. מעבר טאב = שמירה+טעינה (`snapshotActiveDesign`/`loadDesignIntoActive`/`switchToSlot`). `natDims`/`traceStickerPath`/`shapeSvgMarkup`/`renderStickerContent`/`buildTile` מקבלים `design`/`designLayers` אופציונליים בברירת מחדל = הגלובלי, כך שמצב סוג-יחיד זהה ביט-לביט. `packingIn`/`layoutIn`/`dualLayout` תוספת חדשה ליד `packing`/`layout` הקיימות. קו הפרדה נגרר על `a4Canvas`, קלאמפ שמבטיח לפחות שורה אחת לכל סוג (`splitFractionValid`). **באג אמיתי שנמצא ותוקן:** שכבת תמונה מחזיקה `l.image` (DOM `Image`) — `JSON.parse(JSON.stringify(layers))` הופך אותו ל-`{}` בשקט; נוסף `cloneLayersPreservingImages`. **חריגה מהמפרט:** קו ההפרדה הוצא בכוונה מפלטי PDF/SVG — כלי עריכה בלבד. **⛔ מחוץ להיקף (PR B):** סיכום ההזמנה. אומת מלא ב-Playwright headless | #107 | 03/09 |
| מדבקות — **תיקון קריטי: הפרדת עריכה מתצוגה מקדימה בקבוצות חוזרות + מרוץ טעינת פונט בתמונות ממוזערות**. `.preview-wrap` הוסרה מ-`free-mode-hide`; ציור ההרכבה עבר מ-`drawSingle()` ל-`drawSheetTo()` (מקור אמת יחיד לתצוגה ולהדפסה). `singleCanvas` חזר להיות משטח עריכה בלבד. תוקן גם מיפוי שכבות חופשיות (`freeContentBoxFor` משותפת) ומרוץ טעינת פונט בתמונות ממוזערות של קבוצות (`renderRepeatGroupsList` הפכה ל-async). **הערת תהליך:** התיקון הראשון נכתב כבר במהלך #109 אך המיזוג קרה לפני שהפוש הגיע — שוחזר כאן ב-cherry-pick. | #111 | 04/09 |
| מדבקות — **תיקון קריטי: אריח מטמון ישן בגודל שגוי + טקסט נחתך במדבקות עגולות**. `tileSizeAtBuild[slot]` משווה `L.w`/`L.h` בפועל מול המידה שבה האריח נבנה בכל קריאה, לצד ההגנה הידנית הקיימת ב-`drawSingle`. **חריגה מהמפרט — שתי ההיפותזות שניתנו לא היו נכונות:** שורש הטקסט-הנחתך היה ב-`layoutText()`, שבדקה רק גובה ומעולם לא רוחב מול `boxW` עצמו כשאין `boxWidth` מפורש — `shape='circle'` חרג פי 1.19 מ-`boxW` בכל גודל פיזי (לא רק במדבקות קטנות). תוקן גם בזרימת הלקוח הרגילה (אותה `layoutText` משותפת). | #112 | 04/09 |
| מדבקות — **מהירות קו ההפרדה + ספירה חיה + אזור תפיסה מגע**. `applySplitCandidate()` מפרידה "מה שזז חלק" (`drawDividerOverlay` על גבי צילום `a4SnapshotCanvas`) מ"מה שכבד" (`scheduleSplitRecalc`, 80ms debounce ייעודי). חריגה מהמפרט: קריאה ל-`schedSheet()` בתוך הטיימר הייתה מכפילה השהיה ל-80+300ms — קוראים ל-`drawSheet()` ישירות; `pointerup` מפעיל `flushSplitRecalc()` מיידי. נוספו ספירה חיה + נודג' (`SPLIT_STEP=0.02`) מסונכרנים עם הגרירה. תוספת תוך כדי: `dividerHitToleranceInCanvasPx()` ממירה סובלנות תפיסה ליחס תצוגה בפועל (20px-קנבס קבוע היה מתכווץ לכ-6px מסך בטלפון) + פס-רקע חזותי תואם. | #113 | 04/09 |
| מדבקות — **תיקון ארכיטקטוני: יעד עריכה גלוי בעריכה החופשית**. פס קבוע `#edit-target-indicator` בראש האזור (מעל פאנל ההוספה) + צביעת `#add-content-panel` (`.editing-group`) — מציגים למשתמש במפורש אם תוכן חדש הולך לתוך התוכן החופשי הבסיסי (`activeGroupId===null`) או לתוך יחידת-קבוצה שנערכת כרגע. עדכון דרך `updateEditTargetIndicator()`, נקרא מ-`updateGroupEditVisibility()` (הפונקציה היחידה שכבר רצה בכל שינוי `activeGroupId`) + מ-listener נפרד של שדה הכמות (עדכון חי בזמן הקלדה). **חריגה מהמפרט:** כפתור היציאה החדש קיבל `id="edit-target-exit-btn"` ולא `exit-group-edit-btn` כפי שבמפרט — id כזה כבר תפוס ע"י כפתור קיים בפאנל הקבוצות, ושני אלמנטים עם אותו id הם באג DOM אמיתי (`getElementById` היה מחזיר רק את הראשון). אומת ב-Playwright: מעבר בין שני ההקשרים משנה את הפס מיד, תוכן חדש נוסף ליעד הנכון בכל פעם, שני כפתורי היציאה (הישן והחדש) עובדים ומסונכרנים, זרימת לקוח רגילה ללא שינוי. הורצה גם חבילת הרגרסיה המלאה (5 קבצי בדיקה קודמים) — ללא רגרסיות חדשות. | #115 | 04/09 |
| עמוד ניהול (`stickers-admin.html`) — **מחיקת הזמנות + הורדת PDF בכרטיס**. כפתור `🗑️ מחק הזמנה`: קבצים (svg/preview/print) נמחקים מ-Storage **לפני** השורה ב-DB, לא להפך — כשל חלקי במחיקת קבצים לא עוצר (`console.warn` בלבד), רק כשל במחיקת השורה עצמה עוצר ומציג `alert`. כפתור `📄 הורד PDF`: אין PDF שמור בפועל — `print_path` הוא JPEG (`buildPrintFile` בלקוח); הכפתור עוטף אותו ב-PDF A4 בצד המנהל בזמן ההורדה (jsPDF, `addImage(...,0,0,210,297)` — תואם בדיוק ליחס A4 של הקנבס שיוצר את ה-JPEG, בלי עיוות), בלי לגעת בקובץ המאוחסן; מושבת כשאין `print_path`. שני הכפתורים מחוברים דרך אותו דפוס event-delegation הקיים בקובץ (לא מאזין חדש). **חריגות מהמפרט:** (1) לא הועתקו מחלקות `btn`/`btn-danger`/`btn-primary` מהמפרט — הן לא קיימות ב-CSS של הקובץ הזה (שייכות לקבצים אחרים בפרויקט); נוספו `.btn-delete`/`.btn-pdf` מותאמות לשפה החזותית הקיימת כאן. (2) לא נוסף `data-oid` לכפתורים כמו במפרט — מיותר, ה-handler הקיים כבר פותר את ה-order מ-`closest('.order-card').dataset.id`. (3) נוספה טיפול-שגיאה סביב טעינת התמונה ב-`downloadOrderPDF` (המפרט השאיר Promise לא-מטופל). התשתית ב-Supabase (RLS ל-DELETE, מוגבלת ל-hoshaya@gmail.com) כבר קיימת, לא נוגע ב-SQL. אומת ב-Playwright עם stubs מקומיים ל-Supabase/jsPDF (הרשת האמיתית ל-Supabase חסומה מהסביבה הזו ממילא, וגם ה-CDN היה לא יציב): מחיקה מסירה מהרשימה + קוראת ל-storage.remove עם כל שלוש הנתיבים + ל-DB delete עם ה-id הנכון; ביטול הדיאלוג לא עושה כלום; כשל ב-storage לא חוסם מחיקת השורה (מאומת ישירות); כשל ב-DB עוצר ומציג alert (מאומת ישירות); PDF מושבת בלי print_path; הורדת PDF קוראת ל-signedUrl עם הנתיב הנכון ול-`addImage` עם המידות המלאות A4. | #116 | 04/09 |
| עמוד ניהול (`stickers-admin.html`) — **שולם+הודפס כשני מצבים עצמאיים + חיפוש לפי טלפון**. שני checkbox נפרדים (`.paid-toggle`/`.printed-toggle`) מעדכנים ישירות את עמודות `paid`/`printed` בטבלה (עמודת `printed` חדשה, נוספה מראש ב-DB — לא נוגע ב-SQL) — הזמנה יכולה להיות משולמת ולא מודפסת או להפך, בלי תלות זו בזו. `status` נשאר בטבלה אך **לא** משמש יותר להצגת שולם/הודפס. **חריגה מהמפרט (החלטה ארכיטקטונית, לא רק יישום):** הוסרו כפתורי `✓ שולם`/`🖨️ הודפס` הישנים מ-`order-status-actions` (`setStatus` הפשיטה בהתאם — הסניף המיוחד ל-`newStatus==='paid'` הוא קוד מת, אין קורא לו יותר) — השארתם הייתה יוצרת שני מנגנונים חופפים עם תוויות כמעט זהות שמעדכנים עמודות שונות לגמרי, בדיוק הבעיה שה-PR הזה בא לפתור (נשאר רק `✔️ בוצע`, מצב חיים נפרד שלא קשור ל-paid/printed). **חריגה נוספת:** `togglePaid`/`togglePrinted` מקבלות את אלמנט ה-checkbox ומשחזרות אותו (`checked=!val`) אם העדכון נכשל בשרת — במפרט הנתון הכשל היה משאיר את ה-checkbox מסומן ויזואלית (הדפדפן כבר שינה אותו) בלי שהערך השתנה ב-DB, עד לרינדור הבא; זה עוקב אחרי התקדים הקיים כבר ב-`setStatus` בקובץ הזה (שחזור אופטימי בכשל). חיפוש טלפון: סינון בצד לקוח בתוך `renderOrders()` הקיימת (לצד סינון סטטוס, לא במקומו), משווה substring על ספרות בלבד; **חריגה:** מנרמל גם את `customer_phone` עצמו (`replace(/\D/g,'')`) לפני ההשוואה, לא רק את קלט החיפוש כמו במפרט — כך שמספר שמור עם מקפים/רווחים עדיין נמצא (אומת ישירות עם `050-111-2222`). אומת ב-Playwright: שני ה-checkbox עצמאיים בשני הכיוונים (סימון/ביטול אחד לא נוגע בשני, גם כש-DB call נכשל ומשחזר), הכפתורים הישנים נעלמו, חיפוש חלקי מסנן נכון וניקוי מחזיר הכול. | #117 | 04/09 |
| עמוד ניהול (`stickers-admin.html`) — **תיקון: פילטר סטטוס לפי פעולה נדרשת, "בוצע" נגזר אוטומטית**. הפילטר הפך מ"הכל/חדש/שולם/הודפס/בוצע" ל"הכל/חדש/**לא שולם**/**לא הודפס**/בוצע" — מציג מה עוד צריך לעשות, לא מה שכבר הושלם. `matchesStatusFilter(o,filter)` חדשה בודקת רק `o.paid`/`o.printed` (`o.status` לא נבדק בכלל, גם לא בשביל `'new'`). כפתור `✔️ בוצע` **הוסר לגמרי** — "בוצע" הוא מצב נגזר (שולם וגם הודפס), לא פעולה ידנית; `setStatus()` (קוד מת ללא קוראים) ובלוק ה-`[data-status]` בהאצלת הלחיצות הוסרו יחד עמו, וכן ה-CSS הנפרד ל-`.order-status-actions` (מוזג לתוך `.order-actions`). **חריגה מהמפרט (סעיף 4):** ה-badge הצבעוני בראש הכרטיס (שהיה קורא מ-`STATUS_META[o.status]`) עודכן ל-`derivedStatusKey(o)` חדשה — לא רק שני המצבים המפורשים שהמפרט ציין (`'done'`/`'new'`, עוברים דרך `matchesStatusFilter` כמו הפילטר עצמו), אלא גם שני מצבי-הביניים (שולם-בלבד/הודפס-בלבד) שלא הוזכרו במפרט אך חייבים תווית — נבדקים ישירות על `paid`/`printed` (בלי `not-paid`/`not-printed`, קטגוריות פילטר רחבות מדי לתווית מדויקת), משתמשים מחדש באותם `label`/`color` שכבר קיימים ב-`STATUS_META`. אומת ב-Playwright: כל ארבעת השילובים (חדש/שולם-בלבד/הודפס-בלבד/בוצע) מסוננים ומתויגים נכון; מעבר חי לשילוב מלא (שני ה-checkbox יחד) מוציא הזמנה מ-not-paid/not-printed/new ומכניס אותה ל-done באותו רגע כולל עדכון ה-badge; שילוב פילטר+חיפוש טלפון יחד (AND) מסנן נכון; אפס `[data-status]`/`.order-status-actions` שרידים ב-DOM, `setStatus` לא קיים יותר. | #118 | 04/09 |
| מדבקות — **PR גדול: ביטול העריכה החופשית (refactor+deprecation, לא רק תיקון)**. החלטת ארכיטקטורה עם הושעיה: מיקום חופשי של אלמנטים על דף (`mode=free`) **בוטל לגמרי** — "מדבקה בגודל הדף" כבר נתמכת דרך הזרימה הרגילה (מגבלת "מדבקה אחת ממלאת דף" קיימת מהתחלה), כך שלא היה עוד צורך בשני מסלולי קוד למשהו שהמסלול הרגיל כבר תומך בו. זה גם סוגר לצמיתות את כל שרשרת הבאגים (#111/#112/#115) לא בעוד תיקון אלא בביטול המקור: שני מצבי תוכן (חופשי/קבוצה) שחלקו ממשק בלתי-ברור. **נמחק כליל:** `isFreeMode()`, `freeLayoutBox()`/`freeContentBoxFor()`, פאנל "מצב חיתוך" (`free-cut-panel`/`marginRectFor`/`drawMarginRect`/`buildFreeCutSvg`/`freeCutMode`/`freeMarginMm`), `exportFreeLayoutPDF`, `#edit-target-indicator`/`updateEditTargetIndicator()` (#115 — הבלבול שהם פתרו כבר לא קיים ברגע שאין יותר שני מצבים), מחלקות `free-mode-only`/`free-mode-hide` וכל השימושים בהן, קישור "🖼️ עריכה חופשית להדפסה". **נשמר ועבר לעורך הרגיל, מאחורי `?admin=1`:** "קבוצות חוזרות" (`repeatGroups`/`layoutAllGroups`/`shelfFor`/`switchGroupEditTarget`/`tileSizeAtBuild` — ללא שינוי פנימי) — מופעלות עכשיו דרך כפתור חדש "🔁 קבוצות מרובות" (`repeatGroupsActive`, משתנה זמן-ריצה, לא URL) במקום `mode=free`. `regularDesignBackup`/`snapshotActiveDesign`/`loadDesignIntoActive` חדשים שומרים/משחזרים את העיצוב הרגיל בהחלפת מצב, כמו שכבר קורה לטאב סוג-שני. `export-svg`/`export-print-pdf` הורחבו לדעת לייצא גם קבוצות (`buildCutSvg` ענף חדש) — במקום כפתורי `free-export-*` נפרדים שנמחקו. **חריגה מהמפרט (תיקון ארכיטקטוני נדרש עקב runtime-toggle):** `applyAdminVisibility()` הפכה ממעבר-חד-פעמי (URL, לעולם לא הפוך) למעבר יחיד המחשב לכל אלמנט את כל התנאים הרלוונטיים (`admin-only`/`groups-mode-only`/`groups-mode-hide`) יחד — הטריק הישן ("admin-only קודם, free-mode-hide רק מוסיף הסתרה") לא יכול לעבוד יותר כש-`repeatGroupsActive` עצמו הופך ונשאר off/on/off באותה טעינת-דף; אלמנט שגם `admin-only` וגם `groups-mode-hide` (פאנל הדפסה/חיתוך) חייב לחזור גלוי למנהל אחרי כיבוי מצב הקבוצות, לא להישאר תקוע מוסתר. **מה נשאר בדיוק כמו שהיה, לא נוגע בכלל:** זרימת הלקוח הרגילה (single-design, dual-design/פס כחול) — `layout()`, `packingIn`, `applyHeight`, `currentRatio`, `dualLayout`, `drawRegionTiles` הבסיסית. אומת ב-Playwright: `?mode=free` לא עושה יותר כלום (אין URL שקט, אין כפתור); לקוח רגיל מדבקה בודדת/שני-סוגים — אפס שינוי בשני התרחישים; מנהל מפעיל "קבוצות מרובות", מוסיף 3 קבוצות בכמויות שונות, עורך גודל/צורה — זהה למה שעבד בעריכה החופשית לשעבר; תיקון מטמון האריח (#112) ותיקון חריגת-רוחב בעיגול (#112) שניהם עדיין תקפים במיקום החדש; ייצוא SVG עובד תוך כדי מצב קבוצות; כיבוי המצב משחזר את העיצוב הרגיל ואת כל הפאנלים (order-panel/dual-design-panel/panel-print-cut) נכון; חיפוש כללי סופי אחר `isFreeMode`/`freeLayout`/`free-mode` בקובץ — אפס תוצאות מחוץ להערות היסטוריות. | #119 | 05/09 |
| מדבקות — **תמיכה בכמה שורות טקסט (מכתב): Enter לשבירת שורה מפורשת**. `#sel-text-input` הפך מ-`<input type="text">` (לא יכול להכיל Enter כלל) ל-`<textarea>` — `.value`/`.focus()`/`.select()` זהים, אין `<form>` שיגרום ל-submit. `wrapTextLines(ctx,text,maxWidthPx)` חדשה: מפצלת קודם ל-פסקאות לפי `\n` מפורש, ואז עוטפת כל פסקה בנפרד לפי רוחב (אם יש `boxWidth`) — בלי `boxWidth`, כל `\n` הוא בדיוק שורה אחת בלי עטיפה, כך שהלקוחה קובעת בעצמה איפה שוברים (ברכה/גוף/חתימה). פסקה ריקה (Enter כפול) → שורה ריקה נשמרת, לא נבלעת. `width` (למדידה כש-`!boxWidth`) עודכן לרוחב הרחב מכל השורות (היה `lines[0]` בלבד), משמר נכון את `widthOk` מ-#112. **חריגה מהמפרט (בדיקה בפועל חשפה שהמפרט טעה):** המפרט ביקש שלולאת הכיווץ (8 ניסיונות, ×0.92) תישאר בדיוק כמו שהיא — אבל אומת ב-Playwright שהיא **לא** "כבר עובדת לכל `lines.length`" כפי שהמפרט הניח: מכתב ריאליסטי בן 4 שורות (בדיוק תרחיש-המוצא של המשימה) כבר נתקע בגודל לא-מתאים כי 8 ניסיונות לא מספיקים להגיע לגודל שבאמת נכנס (3 שורות בקושי נכנסות בניסיון האחרון). התנאי/הנוסחה עצמם תקינים (לא הניחו שורה אחת) — רק מספר הניסיונות לא הספיק לתוכן ארוך אנכית יותר. הוגדל ל-40 ניסיונות (נבדק עד 8 שורות, כולן נכנסות או מגיעות ל-`fSize≤8`); לא משנה שום מקרה שכבר נכנס לפני כן — יוצא באותו `fSize` מדויק, רק לא נעצר מוקדם מדי. גם `textarea` קיבל את אותו CSS כמו `input[type=text]`/`select` (המפרט לא כלל את זה, הושמט מה-HTML הנתון). אומת ב-Playwright: Enter יוצר שורות נפרדות במיקום הנכון; שילוב `\n`+`boxWidth` צר עוטף כל פסקה בנפרד ושומר על שבירות המקור; Enter כפול שומר שורה ריקה; 2–8 שורות מתכווצות עד שנכנסות; טקסט חד-שורתי ללא שינוי (תאימות לאחור מלאה); לחיצה כפולה עדיין ממקדת את ה-textarea נכון; ייצוא SVG/PDF (`buildCutSvg`/`buildTile`/`drawSheetTo`) עם טקסט מרובה-שורות לא קורס. | #120 | 06/09 |
| מדבקות — **קבוצות חוזרות בעריכה החופשית** (`mode=free` בלבד). כל קבוצה היא בדיוק "מדבקה" (shape/radius/lenMm/widMm/layers/ratio) + `qty`, עריכה דרך `switchGroupEditTarget` (כמו `switchToSlot`). `layoutAllGroups()`/`shelfFor()` פורסות קבוצות כ"מדפים" אנכיים; חריגה מהמפרט לתיקון באג: `shelfFor` הנתונה תמיד החזירה `actualQty===qty` — תוקן לחישוב שורות שלמות שבאמת נכנסות. תוקן גם איבוד פוקוס בשדה כמות. **⚠️ הערה: גרסה זו כפי שמוזגה עדיין נשאה את באג התצוגה שתואר ותוקן ב-#111 (ראה למטה) — התיקון לא הספיק להיכנס ל-PR זה לפני המיזוג.** | #109 | 03/09 |
| מדבקות — **פישוט פאנל הגודל ללקוח: רק גובה בס"מ**. לקוח רגיל רואה רק שדה "גובה (ס"מ)" + חצי `−`/`+` + משפט קצר; שדה האורך הנגזר, ההסבר המלא, הפריסטים ובורר הכיוון עברו ל-`#size-panel-advanced` חדש עם `class="admin-only"` (אותו מנגנון `applyAdminVisibility` קיים). המודל הפנימי (state.widMm וכו') נשאר במ"מ ללא שינוי — ההמרה בגבול התצוגה בלבד (`syncSizeInputs`/listener של `#wid-mm`). | #110 | 04/09 |
| מדבקות — שיפור סיבוב שכבה לנגישות מגע: ידית גדולה (`ROT_OFFSET=30`/`ROT_R=11`, איקון קשת+חץ) + pinch-rotate בשתי אצבעות (`activePointers`, `{capture:true}`). ארבעה באגים אמיתיים נמצאו ותוקנו בבדיקת CDP touch אמיתית: בידוד `pointerId` חסר (`activeDragPointerId`), תנאי הכניסה ל-pinch מהמפרט (`interactionMode===null`) שאף פעם לא הצליח בתרחיש הטבעי (הוחלף ב-`freshMove` מבוסס-תזוזה), `capture:true` שקובע רק סדר לא בלעדיות (נדרש `stopImmediatePropagation`), ואצבע-זרה ששוברת גרירה פעילה (guard חדש בראש `pointerdown`). אומת ב-Playwright/CDP touch אמיתי; ידית העכבר הישנה עדיין עובדת | #108 | 03/09 |
| מדבקות — **תיקון קטן: כפתורי הפריסט חוזרים להיות גלויים ללקוח**. שלושת כפתורי הפריסט (✏️ עפרונות/📐 ממוצע/📓 מחברות) הוצאו מהמיכל `#size-panel-advanced` (admin-only, PR #110) לפני מיכל זה — נשארים גלויים לכל המשתמשים, בעוד שדה האורך/בורר הכיוון/ההסבר הארוך נשארים admin-only. שינוי מיקום ב-DOM בלבד, `applyHeight()` והלוגיקה לא נגעו. אומת ב-Playwright: לקוח רואה פריסטים ולוחץ עליהם בהצלחה (`state.widMm` מתעדכן), `#size-panel-advanced` עצמו עדיין מוסתר ללקוח, תצוגת מנהל ללא שינוי, אפס שגיאות JS | #114 | 04/09 |
| שדה מייל חובה ביצירת מנוי | #70 | 23/08 |
| תיקוני UX: 6 בדף, סליידרים חיים, אישור הורדה | #71 | 23/08 |
| מרשם משימות מרכזי | #72 | 23/08 |
| באג הקוד הכפול — נסגר בצד שרת (claim_access_code v2 + register_free_code v2, פריסה ב-MCP) | — | 23/08 |
| ספריית סמלים — חיפוש עברית + בורר סגנון (כולל תיקון 404 ב-Noto) | #69 | 23/08 |
| עורך גב הקלף נפתח לפי מסלול, לא לפי חשבון Auth | #74 | 23/08 |
| תיקון פיקסלים קשיחים ב-cardGeom/getPatternTile | #77 | 24/08 |
| בורר איכות ייצוא + משקל קובץ (300 DPI, compress:true, JPEG להדפסה) | #76 | 24/08 |
| קובץ עיצוב נייד (ייצוא/ייבוא, בלי תלות בחשבון) | #78 | 24/08 |
| ניסוח מחיר (תשלום חד-פעמי, לא מנוי) + הגהה (מ"מ) | #79 | 25/08 |
| שמירה לקבצים באייפון (navigator.share) | #80 | 25/08 |
| טלמטריה + ניקוי זיכרון | #81 | 25/08 |
| שיתוף לקובץ העיצוב הנייד (navigator.share) | #82 | 25/08 |
| PDF לבית דפוס (עמוד לקלף, בליד 4 מ"מ, 300 DPI) | #83 | 26/08 |
| פתיחת עורך גב הקלף לכל הלקוחות | #84 | 26/08 |
| הודעת מימוש אוטומטי | #121 | 07/09 |
| תיקיית בית דפוס מקופלת + מגש גיליון לכמה משחקים (מנהל בלבד) | #128 | 09/09 |
| שיפוץ פאנל הלקוחות (טבלה מלאה, חיפוש/סינון/מחיקה דו-שלבית) | #129 | 10/09 |

## 🟡 באגים מלקוחות — מאומתים, ממתינים למפרט

| # | תיאור | מקור | חומרה |
|---|---|---|---|
| 4 | מסך המימוש מאפשר לבחור **קוד חדש** במקום לשדרג קוד קיים. לקוחה נרשמה בקוד אחד, שילמה, הקלידה קוד דומה — והעיצוב שלה נשאר תלוי בקוד הישן במסלול 7. **שורש אומת:** ל-claim_access_code חסרה הבדיקה שיש ל-register_free_code. תיקון בשני חלקים: פרונט (PR זה) ואז SQL דרך MCP. אומת ב-DB: לקוחה אחת בלבד הושפעה, וכבר מטופלת. | לקוחה, 21/08 | **קריטי** — יקרה שוב לכל לקוח |
| 5 | 15 מנויים ותיקים בלי מייל לקוח — אי אפשר לקשר תשלום או לשחזר גישה | ביקורת DB | גבוה |
| 6 | אומת: אין הורדה אוטומטית, נחשף קישור `<a download>` שספארי מתעלם ממנו ופותח את ה-PDF בכרטיסייה. נוסף כפתור שיתוף מערכת (navigator.share) שמאפשר "שמירה לקבצים" (PR #80). נוסף דיווח share-available / share-ok / share-fail לטבלת events. אימות ייעשה מנתוני שימוש אמיתיים ולא במכשיר בדיקה — להושעיה אין אייפון. | לקוחה, 04/08 | ממתין לנתוני שימוש |
| 7 | מ"מ הואחד; שגיאת הכתיב נבדקה; פניות בזכר/נקבה/רבים נסרקו ודווחו, טרם טופלו. | סקירה | נמוך |
| 12 | תשלום מ-03/08 שמעולם לא מומש בקוד — הלקוחה שילמה ולא קיבלה גישה. אותר בביקורת DB 24/08. **גבוה — לקוחה משלמת ללא תמורה.** | ביקורת DB, 24/08 | **גבוה** |
| 8 | עורך גב הקלף נחסם ללקוחות שנכנסו בקוד (backEditorAllowed נשען על isSignedIn במקום על המסלול), והוצג להם חלון "מעבר לתשלום" אחרי שכבר שילמו. דווח ע"י לקוחה 23/08. **קריטי.** | לקוחה, 23/08 | **קריטי** |
| 9 | הודעת השגיאה של "🆘 תקוע/ה?" (sendStuckToAdmin) אומרת ללקוח שצריך "להיכנס עם קוד מנוי" — לקוח משלם שנכנס בקוד יבין מזה שהקוד שלו לא תקין. השער עצמו נכון טכנית (auth.uid() נדרש), רק הנוסח מטעה. נמצא בסקירת isSignedIn ב-PR #74. | סקירת קוד, 23/08 | בינוני |
| 10 | לחיצה על עיצוב שמור אצל לקוח בלי חשבון Auth אפסה את S.assets וסיימה ב-schedIdbSave, שדרס את IDB_STORE במאגר ריק — מחיקה קבועה של תמונות הלקוח מהמכשיר. דווח ע"י לקוחה 23/08. **קריטי — אובדן נתונים.** שורש הבעיה נסגר בקובץ העיצוב הנייד (PR זה): גיבוי מלא שאינו תלוי בחשבון. | לקוחה, 23/08 | **קריטי — אובדן נתונים** |
| 11 | ~~אומדן משקל הקובץ סוטה 60–89%, PDF של 21 קלפים 105MB~~ **נפתר** ב-PR #76. | סקירת קוד, 24/08 | נפתר |
| 13 | עורך הגב נפתח ללקוח חינם עם מייל מחובר ונחסם ללקוח חינם בלעדיו — הבחנה ללא קשר לתשלום. דווח ע"י לקוחה 26/08. נסגר בפתיחת הפיצ'ר לכולם. | לקוחה, 26/08 | נפתר |
| 14 | אחרי המימוש האוטומטי (morning-webhook v12), לקוחה שהגיעה למסך המימוש קיבלה "לא נמצא תשלום" למרות ששילמה. דווח 07/09. השרת תוקן ב-MCP; ההודעה בפרונט ב-PR #121. **החצי השני נסגר כאן:** `applySessionTier` רצה רק על `SIGNED_IN` של Auth, ולכן לקוחות שנכנסו בקוד לא ראו לעולם שדרוג שקרה אחרי פתיחת הלשונית. | לקוחה, 07/09 | נפתר |
| 15 | לקוח שנרשם בחינם אינו נמצא בחיפוש לפי טלפון — בהרשמה חינמית לא נאסף טלפון כלל. נוספה הנחיה בממשק (PR זה). פתרון מלא ידרוש איסוף טלפון גם בהרשמה חינם. | ביקורת מנהל, 08/09 | בינוני |

## 🟢 החלטות שנפלו — ממתינות למימוש

- מעבר לאימות **מייל + קישור כניסה בלבד** (בלי סיסמה). קודי הגישה הקיימים ימשיכו לעבוד לצד המייל.
- מסלול חינם **בלי הרשמה** — נכנסים ישר, כפתור רכישה בלבד.
- מדיניות מסלול: חיבור מייל = גיבוי וסנכרון בלבד, **לעולם לא משנה מסלול**. `tierFromGate` מעלה מסלול ולא מוריד.
- הפעלת `Confirm email` בדשבורד Supabase + פריסת `my_access_tier` v2 (עבודת MCP, לא קלוד קוד).
- "מנוי חודשי" הוא שם שגוי — אין חיוב חוזר במערכת. הניסוח בכל הממשק: תשלום חד-פעמי · חודש גישה.
- `manage-customers` מדווח `admin_customer_lookup` בכל פתיחת פאנל — 240 אירועים ב-14 יום, יותר מכל אירועי הלקוחות ביחד. הרעש מקשה על אבחון. להשתיק דיווח כשהחיפוש ריק. פונקציית Edge — פריסה דרך MCP בלבד, **לא לביצוע ע"י Claude Code** (חסום מול Supabase).
- עיצוב גב הקלף פתוח לכל המסלולים. מספר הקלפים נותר ההבדל היחיד בין חינם לתשלום.
- תמחור מ-08/09/2026: 10 ₪ → 21 קלפים, 15 ₪ → 31, 25 ₪ → 57. מדרגת 5 ₪ בוטלה (נטו 3.50 ₪ בלבד). כל מדרגה כוללת את כל החפיסות הקטנות ממנה. `morning-webhook` v13 מכיל גם את הלינקים הישנים בכוונה — לקוחה שפתחה לינק ישן לפני השינוי חייבת עדיין לקבל גישה.
- `window.TIER_INCLUDES` (חדש, ליד `TIER_PRICES` בשורה ~757) הוא מפת-תצוגה בלבד לטקסט "כולל חפיסות של...", **לא נגזר מ-`ORDERS`**: `renderTierPicker` נקראת כבר בשורה 809 (טעינת הדף), בעוד `const ORDERS` מוגדר בבלוק סקריפט 4 (שורה ~1949) — אומת בפועל ב-Chromium ש-`ORDERS` שם עדיין ב-TDZ (`ReferenceError`), וגם `window.ORDERS` לא היה פותר כי גם ההשמה אליו קורית רק בשורה 1949. כל שינוי עתידי במדרגות המחיר חייב לעדכן את `TIER_INCLUDES` יחד עם `TIER_PRICES` — אין ביניהם קישור אוטומטי.
- שחזור קוד נעשה דרך קישור כניסה למייל בלבד. הקוד לעולם אינו נשלח במייל — `my_access_codes` שולפת את הכתובת מ-`auth.jwt()` ולא מקלט, כדי שלא ניתן יהיה לשלוף קוד של לקוח אחר בניחוש כתובת.
- **חריגה מהעוגן המקורי במפרט שחזור הקוד — מאומתת בפועל, לא רק בקריאת קוד:** המפרט הצביע על קריאה יחידה ל-`showMyCodes()` בתוך ה-`then` של `applySessionTier` במאזין `SIGNED_IN` (שורה ~7990 בזמן הכתיבה). אבל אותו `then` רץ **רק** כש-`dobble_unlocked==='1'` — כלומר רק כשהלקוחה כבר בתוך האפליקציה ומחברת חשבון לגיבוי. לקוחה ששכחה קוד ולוחצת על קישור הכניסה **אינה** כבר בתוך האפליקציה, ולכן זורמת לענף האחר של אותו מאזין — `guardedUnlock(session).then(ok=>{...})` — שלא קורא ל-`applySessionTier` בכלל. בלי תיקון, התכונה לא הייתה עובדת אף פעם למקרה השימוש שלשמה נבנתה. נוספה קריאה זהה (עם אותו דגל `_codesShown`) גם בענף `guardedUnlock`. אומת ב-Playwright עם `SB` מדומה: הרצת `SIGNED_IN` על משתמש לא-פתוח (`dobble_unlocked` לא קיים) מציגה את הקוד; שלוש הרצות רצופות מציגות פעם אחת בלבד; `applySessionTier`/`guardedUnlock`/`refreshCodeTier` עצמם לא נגעו בהם — רק בקריאה מהמאזין שסביבם.
- הגבלת הדפסות נדחתה בכוונה. `usage_limit` בטבלה סופר כניסות ולא הדפסות, ומנופח ע"י הרענון האוטומטי (PR #122 קורא ל-`redeem_access_code` בכל טעינת דף/חזרה ללשונית). ייצוא אינו שווה חבילה — לקוחה ייצאה 8 פעמים והכינה משחק אחד. נאסף מספר התמונות בכל ייצוא (`S.assets.length`, בסוף ה-`detail` של אירוע `export` בכל חמשת אתרי הייצוא); ההחלטה על ספים תתקבל אחרי חודש נתונים, על בסיס הצטלבות של הרבה ייצואים **וגם** יותר ממאה תמונות — לא כל אחד לחוד. מונה שימוש (ייצואים/משחקים/תמונות/גיבויים) מוצג בכרטיס הקוד בפאנל, נשלף מ-`events` בקריאה אחת לכל הכרטיסים (לא לכל כרטיס בנפרד), עם ⚠️ תצוגתי בלבד מ-10 ייצואים ומעלה — **אין שום חסימה ביושמה, מדידה ותצוגה בלבד.**
- גיליון בית דפוס: 330×488, שוליים 12, רווח 6, בליד 3. שני קטרים בלבד לפי טבעות הדייקה: 71 מ"מ (24/גיליון) ו-85 מ"מ (15/גיליון, כגודל הדאבל המקורי). 78 מ"מ נפסל — אותה תפוקה כמו 85 בקלף קטן יותר. הפריסה ממורכזת ולכן סימטרית: גיליון הגב מתיישר בהיפוך בלי שיקוף — אומת אלגברית (סימטריה מובטחת ע"י `ox=(W-totalW)/2`, לא רק לשני הקטרים הספציפיים) וגם נבדק במפורש ב-Chromium. `S.sheetDiam` נפרד לגמרי מ-`S.cardSizeMm` (סליידר ההדפסה הביתית) — `expSheetPDF` מחליף את `S.cardSizeMm` זמנית בתוך `try/finally` כדי ש-`drawCard`/`drawCardBack` (התלויים בו בלעדית דרך `cardGeom`) יציירו בגאומטריה הנכונה, ומחזיר אותו גם אם הייצוא נכשל באמצע — אומת ב-Playwright כולל תרחיש שגיאה מאולצת. `expPrintPDF` נשאר ללא שינוי (0 שורות diff).
- **ייצוא בית דפוס עבר לתיקייה מקופלת** (`<details id="printshop-fold">`, מובנה של הדפדפן — אין JS/CSS חדש לקיפול) — שני הכפתורים רלוונטיים למיעוט מהלקוחות.
- **מגש גיליון (כמה משחקים על גיליון אחד) — מנהל בלבד.** נבדק לפני כתיבת קוד ונמצא פער אמיתי: `buildDesignObject`/`applyDesignToState` (המנגנון שהמפרט המקורי הציע) לא שומרים/משחזרים `deck`/`layouts`/`imgEls`/`order` בכלל — הם בנויים לגיבוי-עיצוב-ואז-"ייצר-משחק"-ידני, לא לשחזור-שקט-לרינדור. הפתרון שאושר: `snapshotState()`/`restoreState()` על `SHEET_STATE_KEYS` (המנגנון שכבר הוגדר לגיבוי סביב לולאת הייצוא) משמש גם כפורמט **האחסון** במגש עצמו — "➕ הוסף משחק" דורש דק שכבר נוצר על המסך (כמו ש-`#btn-dosheet` כבר דורש `S.deck?.length`) ומצלם ישירות את שדות ה-`S` החיים, בלי `applyDesignToState`/`generate()` בכלל בלולאת הייצוא. `imgEls` הן הפניות בלבד ל-Image שכבר טעונים (לא עותקים) — אין כפל תמונות בזיכרון.
- **`snapshotState` חייבת העתקה עמוקה, לא `slice()`, ל-`deck`/`layouts`/`order`/`frame`/`back`/`cardEdits`** — לא רק ל-`frame`/`back`/`cardEdits` כפי שתוקן ראשונית. `frame`/`back`/`cardEdits` מקבלים מוטציה **במקום** מהעורכים (לא reassignment) — בלי העתקה עמוקה, משחק שכבר במגש היה משתנה בשקט כשעורכים משחק אחר. אומת ב-Playwright: משחק א׳ (גב אדום) נוסף למגש, נבנה משחק ב׳ (גב כחול, מסגרת שונה) עם מוטציות חיות על `S.back`/`S.frame` — רשומת משחק א׳ במגש נשארה אדומה/מסגרת מקורית ללא שינוי. `deck`/`layouts` נבדקו ישירות מול `genDeck`/`buildLayouts` — דאטה פשוטה בלבד (מספרים/מערכים/אובייקטים שטוחים, בלי הפניות ל-DOM/פונקציות), כך שהעתקה עמוקה בטוחה ובעלות זניחה בגודל הזה.
- **תקרת מגש: 4 משחקים** (`SHEET_TRAY_MAX`) — כל רשומה מחזיקה `imgEls` חיים בזיכרון שלא משוחררים (אין שכפול, אבל גם אין שחרור). מעבר לתקרה → הודעה, בלי הוספה.
- **בדיקת גב לכל משחק בזמן ההוספה, לא רק על הנוכחי** — לחיצת ➕ ללא `S.back.enabled` נדחית עם הודעה מפורשת. משחק בלי גב בגיליון דו-צדדי = מקומות ריקים בגיליון הגב שהיו מתגלים רק אחרי ההדפסה.
- **מיקומים נמשכים ברצף על פני כל המשימות** (`globalIdx` גלובלי, לא מתאפס בין משחקים) וגיליון הגב משתמש באותו רצף מיקומים בדיוק, כדי שכל טווח מיקומים יקבל את הגב של ה-job הנכון. **תוקן במהלך הבדיקה** (לא במפרט): לולאת הגיליונות האחוריים דרשה `addPage()` גם על הגיליון האחורי **הראשון** — לא רק `i>0` כמו בחזית (שם עמוד 1 כבר ניתן חינם מקונסטרוקטור jsPDF). בלי התיקון גיליון הגב הראשון היה מצטייר על גבי עמוד החזית האחרון במקום על עמוד משלו — אומת ב-`addPage` count (5 ולא 4 עבור 52 קלפים ב-2 משחקים) ובמעקב page-per-image מלא.

- **פאנל הלקוחות עבר מכרטיסי-חיפוש לטבלה אחת גוללת/מסוננת.** נשלף פעם אחת (`customers-table`, כבר פרוס ב-`manage-customers` v6) ומסונן אח"כ בזיכרון בלבד — הקלדה בחיפוש והחלפת צ'יפ סינון לא שולחות שום קריאת רשת נוספת (אומת ישירות: מונה הקריאות ל-fetch לא זז). עמודה ראשונה (שם+קוד) מוצמדת בגלילה אופקית עם `position:sticky;right:0` — **ימין ולא שמאל**, כי הדף RTL ועמודה 1 ב-DOM מוצגת בפועל בקצה הימני. צ'יפ "🗑️ לא פעילים" מסונן לפי `safeDelete` (השדה שהשרת מחשב: מעולם לא שילם וגם בלי משחקים/PDF) ולא לפי `is_active` (השבתה/הפעלה ידנית) — שני מושגים שונים בכוונה: האחד "לקוח רדום שאפשר לנקות", השני "קוד חסום ידנית". מחיקה היא דו-שלבית: `confirm()` תמיד, ואז `prompt()` שדורש הקלדת הקוד המדויק **רק** ללקוח לא-`safeDelete` (כלומר שילם או השתמש בפועל) — הקלדה שגויה מבטלת בלי שום קריאת שרת. `admWaLink`/`admTierFromAmount` והרינדור הישן מבוסס-כרטיסים הוסרו כמתים לגמרי (לא נשאר קורא). חריגת פרשנות מהמפרט: פעולת "⬆️ מסלול" מומשה כ-`<select>` בשורה (עם `confirmDialog` באישור), לא ככפתור בודד — כך שכל חמש הדרגות זמינות בלי תפריט נפרד.

- **ארבע נקודות פנייה בוואטסאפ נסקרו 10/09.** נוסח מסך הנעילה שונה מ"אשמח לקבל קוד גישה" ל"יש לי שאלה" — הקודם הזמין פנייה על משהו שהלקוח עושה לבד. כפתור "חידוש מנוי" הוסר לגמרי (החידוש נעשה בלחיצה על מדרגה, השרת מאריך אוטומטית). "תקוע/ה?" נשאר — הוא היחיד שמגיע עם זיהוי מלא. הזמנת הדפסה הוחזרה: נמצא בית דפוס, וייצוא הגיליון מוכן.
- **ממצא שלא היה במפרט: `js/dobble-config.js` הוא מקור-האמת בפועל ל-`contactMessage`, לא ה-`localStorage` override שהמפרט חשד בו.** `window.DOBBLE_CONFIG` (מהקובץ, נטען בשורה 412) גובר על ברירת המחדל בתוך `dobble.html` לפי סדר ה-`Object.assign` — הקובץ החזיק את אותו נוסח ישן בדיוק, כך שלולא עודכן גם הוא, שינוי הנוסח ב-`dobble.html` לא היה משפיע על אף לקוח אמיתי. עודכן במקביל, כולל `?v=2` (כלל ה-cache busting). לעומת זאת, ל-`contactMessage` **אין** נתיב כתיבה ל-`dobble_config_override` ב-`localStorage` בכל הקוד (הכתיבה היחידה דרך `dcfgSave` מטפלת רק ב-`packs`) — כך שהחשש הספציפי מהמפרט לא רלוונטי בפועל.
- **נמצאה נקודת פנייה חמישית שלא נמנתה בטבלת המפרט:** כפתור "📱 פנה/י אלינו בוואטסאפ" במודל הקבלת-פנים (`#onboard-modal`, `id="wa-link"`) תחת הכותרת "לרכישת קוד גישה — צרו קשר" — שריד מהמודל הפתוח-לכולם (freemium) מלפני מודל הגישה המדורג (PR #19), שסותר את עצם הרציונל של הסבב הזה (לקוח יכול לרכוש/לשחזר קוד לבד). לא הוסר ולא נגעתי בו — האם הוא עדיין נחוץ היא החלטת מוצר, לא תיקון תוכן. **כתוצאה מכך `grep -c "wa.me"` מחזיר 4, לא 3 כפי שציפה סעיף 7** (4 נקודות פנייה בפועל אחרי הסרת renew-wa: מסך נעילה, תקוע/ה, הזמנת הדפסה, ונקודה זו שלא נמנתה).

## ⏸️ מוקפא במכוון

- שלבים הבאים לקונסולת ההדפסה: תור בקשות, טעינת .dobble.zip מלקוחות, איחוד כמה לקוחות בגיליון (מחייב גב אחיד), וסימון זיהוי קטן על טבעת המסגרת. ייבנו אחרי ניסיון הדפסה אמיתי.
- CMYK — jsPDF מוציא RGB בלבד; רוב מכונות הדפוס הדיגיטליות ממירות ב-RIP. אם הדפוס יתעקש, זה שינוי ארכיטקטוני נפרד.
- שטנץ עגול — אם לבית הדפוס אין, `S.shape='circle'` לא ניתן לייצור אצלו. אזהרה תתווסף אחרי שהושעיה יקבל תשובה.
- Fluent Emoji (מיקרוסופט) — דורש מפת שמות מאנגלית ל-codepoint.
- מחולל רביעיות + שער AI רב-ספקים (PR #56).
- ניקוי 56 ברנצ'ים מוזגים — ידני דרך אתר GitHub.
- ספירת הברנצ'ים הלא-מוזגים: **15** על קלון מלא מול `189ba64` (אחרי שמיזוג #72/#73 הוריד את שני הברנצ'ים שלהם מהרשימה) — 14 ברנצ'ים ישנים/כפולים כפי שתועד קודם, ועוד `emoji-library` עצמו (PR #69, עדיין פתוח בזמן כתיבת השורה הזו). הרשימה המלאה בתיאור ה-PR. מתוך 6 הרלוונטיים ל-dobble, 5 הוחלפו בעבודה שנחתה ב-main בדרך אחרת. היחיד ששווה להחיות: `dobble-xiaomi-multi-upload-krm39q` — תיקון בחירה מרובה ב-MIUI, מבודד וקטן.

## 📌 כללי עבודה מקבילה

1. `dobble.html` הוא קובץ אחד. **מקסימום שני PRs פתוחים עליו בו-זמנית**, ורק אם הם נוגעים באזורים שונים.
2. כל ברנץ' נפתח מ-`main` עדכני ונסגר תוך 24 שעות. ברנץ' שחי שבוע — הופך לקונפליקט.
3. אחרי כל מיזוג ל-`main`: כל ברנץ' פתוח אחר עושה `rebase` **מיד**, לא כשמגיעים אליו.
4. במפרטים — **עוגנים טקסטואליים מחייבים, מספרי שורות הם רמז בלבד.** הקובץ זז כל יום.
5. אין מיזוג ל-`main` בלי אישור מפורש של הושעיה. push ל-`main` = לייב מיידי.
6. לפני כל פיצ'ר: לשאול אם הוא נשען על isSignedIn(). תשלום מעניק קוד, לא חשבון. שלושה באגים ב-23/08 נבעו מהבלבול הזה.
7. מדידה אמיתית קודמת להערכה תמיד — כשמדד נבנה על אות עקיף (כמו canvas.toBlob כתחליף למשקל PDF), לאמת אותו מול המדד האמיתי לפני שסומכים עליו, לא רק לבדוק שהקוד רץ בלי שגיאה.
8. לפני שקובעים אם PR מתנגש — להשוות מול merge-base ולא בין הקצוות. `git diff --stat $(git merge-base origin/main <branch>) <branch>` מראה מה ה-PR באמת משנה. השוואת קצוות על ברנץ' ישן מציגה את כל מה ש-main הוסיף כאילו ה-PR מוחק אותו — טעות שחזרה שלוש פעמים.
